#make serializers and create normal
# serilizer to return predictions json
#class EndpointSerializer(serializers.ModelSerializer):
 #   class Meta:
  #      model = Endpoint
   #     read_only_fields = ("id", "name", "owner", "created_at")
    #    fields = read_only_fields